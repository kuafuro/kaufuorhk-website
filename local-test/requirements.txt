# 本地字幕 pipeline（Apple Silicon Mac）。Gemini 融合用 stdlib urllib，唔使裝 SDK。
# 另外要系統裝 ffmpeg：  brew install ffmpeg
mlx-whisper                   # Whisper large-v3-turbo（Apple GPU，快好幾倍）
funasr==1.2.6                 # SenseVoice-Small（口語，CPU 飛快）
silero-vad                    # VAD 切片
srt                           # SRT 組裝（冇裝都會自動用 stdlib 砌）
torch==2.4.1                  # funasr / silero 用
torchaudio==2.4.1
modelscope==1.20.1            # funasr 落模型
numpy
